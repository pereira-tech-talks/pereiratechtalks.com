# Task 27: Security Review

## 1. Context

- **Phase:** Final — Mandatory (third-to-last)
- **Plan:** `PLAN_certificate_generation_system`
- **Primary paths:** `analysis_results/SECURITY_REVIEW.md`, `docs/SECURITY.md`
- **Why:** Full-plan security gate. AI Diff Reviewer addon detected (Flow A): `.agents/skills/ai-diff-reviewer/` + `.review/extension.md`.
- **Sensitive surfaces this plan introduces:** Ed25519 signing secrets, public PII names on opaque URLs, did:web, client-side verification, QR URLs, registry files, new public routes.

## 2. Read Before Starting

- Plan README Goal + Global Guidelines.
- `analysis_results/CERT_THREAT_MODEL.md`, ADR, privacy design.
- `docs/SECURITY.md`

## 3. Goal

Review the full accumulated diff for secrets, injection, unsafe input, new attack surface; audit new dependencies; verify SECURITY.md; write SECURITY_REVIEW.md including AI Diff Reviewer local review.

## 4. Instructions

> **Before starting:** Re-read the plan README section 1 (Goal).

1. Diff the full plan branch against the base branch; review certificate scripts, public JSON-LD, verify UI, and CI secret handling.
2. Confirm no private keys, PEM blocks, or attendee emails landed in git.
3. Audit new deps (`qrcode`, crypto libs, etc.) with `pnpm audit` best-effort.
4. Update `docs/SECURITY.md` if the threat model changed.
5. Write `analysis_results/SECURITY_REVIEW.md` (even if clean).
6. **AI Diff Reviewer local review (Flow A):** invoke parent skill *"Review my current branch"*; append under `## AI Diff Reviewer local review`. Critical findings block; warning/info do not. Soft-fail only if skill/extension missing or invocation errors — do NOT skip because `CURSOR_API_KEY` is unset.
7. Do NOT add apply-review as a plan task.

## 5. Acceptance Criteria

- `analysis_results/SECURITY_REVIEW.md` exists and covers the full plan diff.
- No unresolved critical findings (or user-accepted with note).
- `docs/SECURITY.md` updated when surfaces changed.
- AI Diff Reviewer section present OR explicit skip warning per never-block rule.

## 6. Outputs

- `analysis_results/SECURITY_REVIEW.md`
- Possibly updated `docs/SECURITY.md`

## 7. Validation

```bash
test -f .dwp/plans/PLAN_certificate_generation_system/analysis_results/SECURITY_REVIEW.md
# Confirm diff has no private keys
! git grep -nE 'BEGIN (OPENSSH |EC |RSA )?PRIVATE KEY|CERT_SIGNING_PRIVATE_KEY=.+' -- ':!.dwp/**' ':!*.md' || true
```

## 8. Rollback

- Fix findings in follow-up commits; do not delete the security report.

## 9. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Review full plan diff + deps.
- [ ] 3. Write SECURITY_REVIEW.md (+ AI Diff Reviewer section).
- [ ] 4. Run validation.
- [ ] 5. Mark `[x]` in README; update PROGRESS.md.
- [ ] 6. Commit: `git commit -m "docs(security): certificate system security review - Task 27 of PLAN_certificate_generation_system"`
- [ ] 7. Update Log below.

## 10. Completion & Log

- **Status:**
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Validation results:**
- **Notes / follow-ups:**

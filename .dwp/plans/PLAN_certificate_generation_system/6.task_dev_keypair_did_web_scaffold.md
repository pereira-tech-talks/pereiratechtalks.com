# Task 6: Dev Ed25519 keypair and did:web document scaffolding

## 1. Context

- **Phase:** B — Data & Crypto
- **Plan:** `PLAN_certificate_generation_system` — world-class static certificate / diploma system for Pereira Tech Day and future flagship events.
- **Primary paths:** public/.well-known/did.json, scripts/certs-keypair.mjs (optional), docs notes → later CERTIFICATES.md
- **Legacy reference:** [PR #297](https://github.com/pereira-tech-talks/pereiratechtalks.com/pull/297) / `tmp/pr297-cert-branch`
- **Stack constraints:** Astro 7 SSG, bilingual ES/EN, EditionScope brand kits, Cloudflare Pages, Biome, Vitest — no runtime backend for issuance.

## 2. Read Before Starting

- Plan README Goal + ADR/privacy outputs when n>3.
- Task 5 Completion & Log when relevant.
- `AGENTS.md`, `docs/BRAND_GUIDE.md`, `docs/DESIGN.md`, `docs/SECURITY.md` as applicable.
- Refined draft: `.dwp/drafts/PLAN_certificate_generation_system_draft_refined.md`

## 3. Goal

Create developer key material workflow and public `/.well-known/did.json` with ONLY the public key; document production key rotation.

## 4. Instructions

> **Before starting:** Re-read the plan README section 1 (Goal) to ensure this task's work aligns with the overall objective.

1. Generate a DEV keypair locally; store private key ONLY in env example as placeholder name CERT_SIGNING_PRIVATE_KEY — never commit private key bytes.
2. Publish did:web document for pereiratechtalks.org (or preview domain strategy documented).
3. Document how organizers rotate keys and re-sign.
4. Ensure Cloudflare Pages will serve /.well-known/did.json.

## 5. Acceptance Criteria

- did.json committed with public key only; grep confirms no private key material in git.
- Rotation procedure written in analysis_results/CERT_KEY_ROTATION.md (short).
- No secret material in the diff; follows docs/SECURITY.md expectations for this surface.

## 6. Outputs

- public/.well-known/did.json, analysis_results/CERT_KEY_ROTATION.md

## 7. Validation

```bash
test -f public/.well-known/did.json && ! git grep -n 'BEGIN PRIVATE KEY' -- ':!*.md' || true; pnpm run biome:check
```

If validation fails, stop, log, and do not mark the task complete.

## 8. Rollback

- Revert this task's commit with `git revert` if needed.
- Do not delete shared `analysis_results/` artifacts from earlier tasks unless replacing with a corrected version.

## 9. Execution Checklist

- [ ] 1. Read this task file fully and understand the goal and constraints.
- [ ] 2. Inspect related files / prior analysis outputs.
- [ ] 3. Implement the required changes (use browser for UI tasks).
- [ ] 4. Run the validation commands above.
- [ ] 5. Review the diff against acceptance criteria.
- [ ] 6. Mark this task `[x]` in the plan README Task List.
- [ ] 7. Update PROGRESS.md with a short summary.
- [ ] 8. Commit: `git commit -m "type(scope): description - Task 6 of PLAN_certificate_generation_system"`
- [ ] 9. Update the Log section below.

## 10. Completion & Log (filled by the agent)

- **Status:**
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Validation results:**
- **Notes / follow-ups:**

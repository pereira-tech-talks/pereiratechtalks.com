---
/**
 * PTT v3.0.0 badge — tier-aware status indicator. Use for sponsor tiers
 * (diamond/gold/silver/bronze/community), event status (announced/completed
 * /cancelled), and other categorical labels where color encodes meaning.
 */

interface Props {
  variant?:
    | 'diamond'
    | 'gold'
    | 'silver'
    | 'bronze'
    | 'community'
    | 'announced'
    | 'completed'
    | 'cancelled'
    | 'live'
    | 'neutral';
  size?: 'sm' | 'md';
  class?: string;
}

const { variant = 'neutral', size = 'sm', class: className = '' } = Astro.props;

const sizeClass = size === 'sm' ? 'px-2 py-0.5 text-xs' : 'px-2.5 py-1 text-sm';

const variantClass: Record<string, string> = {
  diamond:
    'bg-gradient-to-r from-cyan-100 to-blue-100 text-cyan-900 dark:from-cyan-900/40 dark:to-blue-900/40 dark:text-cyan-200',
  gold: 'bg-amber-100 text-amber-900 dark:bg-amber-900/30 dark:text-amber-200',
  silver: 'bg-gray-200 text-gray-900 dark:bg-gray-700 dark:text-gray-100',
  bronze:
    'bg-orange-100 text-orange-900 dark:bg-orange-900/30 dark:text-orange-200',
  community:
    'bg-emerald-100 text-emerald-900 dark:bg-emerald-900/30 dark:text-emerald-200',
  announced: 'bg-blue-100 text-blue-900 dark:bg-blue-900/30 dark:text-blue-200',
  completed: 'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-300',
  cancelled: 'bg-red-100 text-red-900 dark:bg-red-900/30 dark:text-red-200',
  live: 'bg-red-500 text-white animate-pulse',
  neutral: 'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-200',
};
---

<span
  class={`inline-flex items-center gap-1 rounded-md font-semibold ${sizeClass} ${variantClass[variant]} ${className}`}
>
  <slot />
</span>

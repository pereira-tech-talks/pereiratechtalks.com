---
import { type Language, tr } from '@/lib/i18n';

/** Matches content-collection `i18nString` (plain string or {en,es}). */
type I18nText = string | { en?: string; es?: string };

interface Plan {
  tier: string;
  title: I18nText;
  subtitle?: I18nText;
  price: string;
  period?: I18nText;
  featured?: boolean;
  benefits: I18nText[];
  ctaLabel: I18nText;
  ctaUrl: string;
}

interface PartnershipItem {
  title?: I18nText;
  description?: I18nText;
  subtitle?: I18nText;
}

interface Partnership {
  title: I18nText;
  subtitle?: I18nText;
  items: PartnershipItem[];
  ctaLabel: I18nText;
  ctaUrl: string;
}

interface Props {
  lang: Language;
  title: string;
  plans: Plan[];
  partnerships: Partnership[];
  sectionBg?: string;
}

const { lang, title, plans, partnerships, sectionBg } = Astro.props;
---

<section
  class="relative py-[var(--ptd-section-pad)]"
  style={sectionBg ? `background-image: url('${sectionBg}'); background-size: cover;` : undefined}
  aria-labelledby="ptd-pricing-title"
>
  {
    sectionBg && (
      <div class="absolute inset-0 bg-[var(--ptt-bg)]/90" aria-hidden="true"></div>
    )
  }
  <div class="relative main-container">
    <h2 id="ptd-pricing-title" class="text-3xl font-bold tracking-tight text-[var(--ptt-text)]">
      {title}
    </h2>
    <ul class="mt-10 grid gap-6 lg:grid-cols-3">
      {
        plans.map((plan) => (
          <li
            class:list={[
              'flex flex-col rounded-2xl p-6 ring-1 ring-[var(--ptt-border)] bg-[var(--ptt-bg-elevated)]',
              plan.featured && 'ring-2 ring-[var(--ptt-accent)]',
            ]}
          >
            <p class="text-xs font-semibold uppercase tracking-widest text-[var(--ptt-accent)]">
              {plan.tier}
            </p>
            <h3 class="mt-2 text-xl font-bold text-[var(--ptt-text)]">{tr(plan.title, lang)}</h3>
            {
              plan.subtitle && (
                <p class="mt-1 text-sm text-[var(--ptt-text-muted)]">{tr(plan.subtitle, lang)}</p>
              )
            }
            <p class="mt-4 text-3xl font-bold text-[var(--ptt-primary)]">
              ${plan.price}
              {
                plan.period && (
                  <span class="text-sm font-normal text-[var(--ptt-text-muted)]">
                    {tr(plan.period, lang)}
                  </span>
                )
              }
            </p>
            <ul class="mt-4 flex-1 space-y-2 text-sm text-[var(--ptt-text-muted)]">
              {plan.benefits.map((b) => (
                <li class="flex gap-2">
                  <span class="text-[var(--ptt-accent)]" aria-hidden="true">✓</span>
                  {tr(b, lang)}
                </li>
              ))}
            </ul>
            <a
              href={plan.ctaUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="mt-6 inline-flex justify-center rounded-full bg-[var(--ptt-primary)] px-5 py-3 text-sm font-semibold text-white transition hover:opacity-90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--ptt-primary)]"
            >
              {tr(plan.ctaLabel, lang)}
            </a>
          </li>
        ))
      }
    </ul>
    {
      partnerships.length > 0 && (
        <ul class="mt-10 grid gap-6 lg:grid-cols-2">
          {partnerships.map((p) => (
            <li class="rounded-2xl bg-[var(--ptt-bg-elevated)] p-6 ring-1 ring-[var(--ptt-border)]">
              <h3 class="text-lg font-bold text-[var(--ptt-text)]">{tr(p.title, lang)}</h3>
              {
                p.subtitle && (
                  <p class="mt-1 text-sm text-[var(--ptt-text-muted)]">{tr(p.subtitle, lang)}</p>
                )
              }
              <ul class="mt-4 space-y-2 text-sm text-[var(--ptt-text-muted)]">
                {p.items.map((item) => (
                  <li>
                    {
                      item.title ? (
                        <p class="font-semibold text-[var(--ptt-text)]">{tr(item.title, lang)}</p>
                      ) : item.subtitle ? (
                        <p class="italic">{tr(item.subtitle, lang)}</p>
                      ) : (
                        <p>{tr(item.description, lang)}</p>
                      )
                    }
                  </li>
                ))}
              </ul>
              <a
                href={p.ctaUrl}
                target="_blank"
                rel="noopener noreferrer"
                class="mt-6 inline-flex rounded-full border border-[var(--ptt-primary)] px-5 py-2 text-sm font-semibold text-[var(--ptt-primary)] transition hover:bg-[var(--ptt-primary)] hover:text-white focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--ptt-primary)]"
              >
                {tr(p.ctaLabel, lang)}
              </a>
            </li>
          ))}
        </ul>
      )
    }
  </div>
</section>
